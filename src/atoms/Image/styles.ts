import styled from '@emotion/styled/macro'

const ImageWrap = styled.span`
  display: inline-block;
  line-height: 0;
`

const Image = styled.img`
  max-width: 100%;
  height: auto;
`

const Styled = { Image, ImageWrap }

export default Styled
