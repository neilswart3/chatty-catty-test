import styled from '@emotion/styled/macro'

const TextFieldWrapper = styled.div`
  display: flex;
  align-items: flex-end;
  gap: 1rem;

  svg {
    margin-bottom: 0.25rem;
  }
`

const Styled = { TextFieldWrapper }

export default Styled
