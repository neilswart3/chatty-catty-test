import React from 'react'
import Styled from './styles'

interface Props {
}

const Avatar: React.FC<Props> = (props) => {
  return (
    <Styled.Avatar>Avatar component</Styled.Avatar>
  );
};
export default Avatar
