import React from 'react'
import Styled from './styles'

interface Props {
}

const Test: React.FC<Props> = (props) => {
  return (
    <Styled.Test>Test component</Styled.Test>
  );
};
export default Test
