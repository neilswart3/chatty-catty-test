import styled from '@emotion/styled/macro'

const Welcome = styled.div`
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
  padding: 3rem 3rem 4rem;
`

const Styled = { Welcome }

export default Styled
